import { DayGameUserPoints } from './day-game-user-points';

describe('DayGameUserPoints', () => {
  it('should create an instance', () => {
    expect(new DayGameUserPoints()).toBeTruthy();
  });
});
